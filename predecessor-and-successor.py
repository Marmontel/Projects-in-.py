# Objective: show the predecessor and successor of a number

number = int(input('Write number: '))
print('The predecessor of {} is {} and the successor is {}.'.format(
    number, (number - 1), (number + 1)))
